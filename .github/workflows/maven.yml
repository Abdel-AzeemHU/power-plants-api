name: API Test Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  API_PORT: 9000

jobs:
  test-api:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3. Install dependencies
      - name: Install dependencies
        run: |
          npm install -g yarn
          yarn install --frozen-lockfile

      # 4. Start the API (in the correct directory)
      - name: Start API
        run: |
          cd opt/api  # CHANGE THIS to your actual API directory
          make api-start > api.log 2>&1 &
          echo $! > api.pid
          
          # Wait for API to be ready
          timeout=60
          while [ $timeout -gt 0 ]; do
            if curl -f http://localhost:${{ env.API_PORT }}/api-docs; then
              echo "API is ready!"
              break
            fi
            sleep 2
            timeout=$((timeout-2))
            echo "Waiting for API to start..."
          done
          
          if [ $timeout -le 0 ]; then
            echo "API failed to start"
            cat api.log
            exit 1
          fi

      # 5. Run your API tests
      - name: Run tests
        run: |
          # Your test command here
          echo "Running tests..."

      # 6. Cleanup (always runs)
      - name: Stop API
        if: always()
        run: |
          if [ -f opt/api/api.pid ]; then  # CHANGE THIS to match your API directory
            kill -9 $(cat opt/api/api.pid) || true
            rm opt/api/api.pid
          fi